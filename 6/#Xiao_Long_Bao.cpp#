#include<iostream>
#include<algorithm>

using namespace std;

int main(){
    int ans = 0;
    cin >> n;
    for(int i = 0;i < n;i++)cin >> d[i];
    for(int j = 0;j < n;j++)cin >> a[j];
    for(int i = 0;i < 
    cout << ans << endl;
    
    return 0;
}
